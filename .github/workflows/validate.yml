name: Validate

on:
  push:
  pull_request:

jobs:
  hygiene:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run hygiene validation
        shell: pwsh
        run: ./scripts/validate.ps1
